__pycache__/
*.pyc
*.db
EDA/
Pitching Stats/
Season Stats/
*.png
.DS_Store

# Secrets — never commit API keys / credentials. Load from env vars (e.g. ODDS_API_KEY).
.env
.env.*
*.key
*.pem
secrets*.json
credentials*.json

# Runtime job-status state (written by app.py, per-instance)
Databases_and_logs/job_status.json
archive/
